/*!
 * Built with Duet Design System
 */
function isInternetExplorer() {
  const ua = navigator.userAgent || navigator.vendor;
  return "documentMode" in document || /Edge/.test(ua);
}

export { isInternetExplorer as i };
