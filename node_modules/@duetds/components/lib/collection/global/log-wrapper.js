/*!
 * Built with Duet Design System
 */
import { Build } from "@stencil/core";
export const setupLoggers = () => {
  if (!Build.isDev) {
    globalThis.ds = {
      log: () => {
        return;
      },
      warn: () => {
        return;
      },
      info: () => {
        return;
      },
    };
  }
  else {
    const dsLog = (...args) => {
      console.log(...args);
    };
    const dsWarn = (...args) => {
      console.warn(...args);
    };
    const dsInfo = (...args) => {
      console.warn(...args);
    };
    globalThis.ds = {
      log: dsLog,
      warn: dsWarn,
      info: dsInfo,
    };
  }
};
