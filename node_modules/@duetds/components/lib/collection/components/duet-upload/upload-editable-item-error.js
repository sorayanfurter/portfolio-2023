/*!
 * Built with Duet Design System
 */
import { h } from "@stencil/core";
import { getI18nError } from "./errorcodes.utils";
export const ErrorItem = ({ data, errorCodes }) => {
  const { item, error } = data;
  const { name } = item;
  const { type } = error;
  return (h("span", { class: "duet-upload-item-error", role: "status" }, h("duet-paragraph", { class: "duet-upload-item-name", color: "danger", margin: "none", weight: "semi-bold" }, h("duet-icon", { margin: "none", size: "x-small", name: "messaging-attachment" }), h("duet-spacer", { size: "xx-small", direction: "horizontal" }), name), h("duet-paragraph", { class: "duet-upload-item-error-label", size: "small", margin: "none", color: "danger" }, getI18nError(type, errorCodes))));
};
