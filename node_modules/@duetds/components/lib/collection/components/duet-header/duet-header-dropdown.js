/*!
 * Built with Duet Design System
 */
import arrowDownIcon from "@duetds/icons/lib/assets/action-arrow-down-small";
import { h } from "@stencil/core";
import { DuetHeaderIcon } from "./duet-header-icon";
export const DuetHeaderDropdown = (props, _children) => {
  const accessibleLabel = props.accessibleText ? props.accessibleText : props.label;
  return (h("div", { class: { ["duet-header-dropdown"]: true, [props.className]: !!props.className } }, h("button", { ref: props.toggleRef, "aria-haspopup": "listbox", "aria-controls": props.id, "aria-expanded": props.isOpen ? "true" : "false", "aria-labelledby": `${props.id}-accessible-label`, id: `${props.id}-button`, type: "button", class: {
      [props.id]: true,
      "duet-header-dropdown-toggle": true,
      "duet-header-button": true,
      active: props.active,
      open: props.isOpen,
    }, onClick: props.onToggle }, props.icon && h(DuetHeaderIcon, { icon: props.icon }), h("span", { "aria-hidden": "true", id: `${props.id}-label`, class: "duet-header-dropdown-label" }, props.label), props.badge && h("div", { class: "duet-header-badge" }), h("div", { class: "duet-header-caret", "aria-hidden": "true" }, h("duet-icon", { margin: "none", size: "auto", icon: arrowDownIcon.svg, color: "currentColor" })), h("duet-visually-hidden", { id: `${props.id}-accessible-label` }, accessibleLabel)), h("div", { ref: props.itemsRef, tabindex: "-1", role: "menu", id: props.id, "aria-labelledby": `${props.id}-button`, class: {
      "duet-header-dropdown-content": true,
      [`${props.id}-items`]: true,
      active: props.isOpen,
    } }, _children)));
};
