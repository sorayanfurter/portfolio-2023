/*!
 * Built with Duet Design System
 */
import { h } from "@stencil/core";
export const DuetHeaderIcon = (props, _children) => {
  const iconSize = props.iconSize ? props.iconSize : "x-small";
  return (h("span", { class: `duet-header-icon ${props.class ? props.class : ""}` }, h("duet-icon", { margin: "none", size: iconSize, icon: props.icon, color: "currentColor" })));
};
