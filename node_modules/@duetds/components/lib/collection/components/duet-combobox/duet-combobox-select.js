/*!
 * Built with Duet Design System
 */
import { h } from "@stencil/core";
import { SelectSingle } from "./duet-combobox-select-single";
export const DuetComboBoxSelect = ({ item, active, total, selected, label = undefined, clickHandler = e => {
  e.preventDefault();
}, keyHandler = e => {
  e.preventDefault();
}, search, }) => {
  return (h("li", { class: {
      "list-item": true,
      option: true,
      active: active,
      selected: selected,
    }, id: active ? "active_item" : "", "data-index": item.id, "aria-selected": active ? "true" : undefined, "aria-current": selected ? "true" : undefined, role: "option", "aria-label": label || `${item.name}, (${item.id + 1} of ${total})` }, h("a", { tabindex: "-1", onClick: e => clickHandler(e), onKeyUp: e => keyHandler(e), class: "option-link", "aria-label": label || `${item.name}, (${item.id + 1} of ${total})`, "aria-hidden": "true" }, h(SelectSingle, { item: item, search: search, selected: selected }))));
};
