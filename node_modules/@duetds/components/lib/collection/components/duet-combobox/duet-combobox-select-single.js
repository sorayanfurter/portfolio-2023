/*!
 * Built with Duet Design System
 */
import { Fragment, h } from "@stencil/core";
export const SelectSingle = ({ item, search = "", selected }) => {
  const regEx = search !== "" ? new RegExp("(" + search.split(" ").join("|") + ")", "gi") : undefined;
  return (h(Fragment, null, selected && h("duet-icon", { class: "option-icon", name: "messaging-checked", size: "xx-small", margin: "none" }), regEx ? (h("span", { class: "option-container", "aria-hidden": "true" }, h("span", { innerHTML: item.name.replace(regEx, '<span class="highlight">$&</span>') }))) : (item.name), item.tags && h("span", { class: "option-tags" }, item.tags.join(", "))));
};
