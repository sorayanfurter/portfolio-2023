import { ComponentInterface } from "../../stencil-public-runtime";
import { DuetListBreakpoint } from "../../common-types";
/**
 * @slot label - The list item component’s label content. Use this to put content into description list’s dt element.
 * @slot value - The list item component’s value content. Use this to put content into description list’s dd element.
 */
export declare class DuetListItem implements ComponentInterface {
  /**
   * @internal
   * Enable or disable the automatic responsive behaviour of the list item.
   * This property is used internally by List, and should not be set by the developer.
   */
  responsive: boolean;
  /**
   * @internal
   * Switch the breakpoint used to trigger the content stacking.
   * This property is used internally by List, and should not be set by the developer.
   */
  breakpoint: DuetListBreakpoint;
  /**
   * render() function
   * Always the last one in the class.
   */
  render(): any;
}
