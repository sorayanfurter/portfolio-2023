import { FunctionalComponent } from "../../stencil-public-runtime";
import { DuetLanguage, DuetTheme, DuetTooltipDirection } from "../../common-types";
import { DuetInputEvent } from "../duet-input/duet-input";
type DatePickerInputProps = {
  value: string;
  valueAsDate: Date;
  formattedValue: string;
  language: DuetLanguage;
  placeholder: string;
  name: string;
  identifier: string;
  inputLabel: string;
  disabled: boolean;
  theme: DuetTheme;
  error: string;
  role: string;
  required: boolean;
  tooltip: string;
  tooltipDirection: DuetTooltipDirection;
  labelHidden: boolean;
  accessibleActiveDescendant: string;
  accessibleControls: string;
  accessibleOwns: string;
  accessibleDescribedBy: string;
  echoPlaceholder: boolean;
  caption: string;
  onClick: (event: MouseEvent) => void;
  onInput: (event: CustomEvent<DuetInputEvent>) => void;
  onBlur: (event: CustomEvent<DuetInputEvent>) => void;
  onFocus: (event: CustomEvent<DuetInputEvent>) => void;
  buttonRef: (element: HTMLButtonElement) => void;
  inputRef: (element: HTMLDuetInputElement) => void;
};
export declare const DatePickerInput: FunctionalComponent<DatePickerInputProps>;
export {};
