import { DuetTheme } from "../../common-types";
import { ThemeableComponent } from "../../common/themeable-component";
export declare class DuetSlideoutLink implements ThemeableComponent {
  /**
   * Own Properties.
   */
  /**
   * Reference to host HTML element.
   */
  element: HTMLElement;
  /**
   * Adds accessible label for the button that is only shown for screen readers.
   * Typically, this label text replaces the visible text on the button for
   * users who use assistive technology.
   */
  accessibleLabel: string;
  /**
   * Theme.
   */
  theme: DuetTheme;
  /**
   * Language of the link. Use this only for language selection links.
   */
  targetLanguage: "en" | "fi" | "sv";
  /**
   * Active
   */
  active: boolean;
  /**
   * Href
   */
  href: string;
  /**
   * Icon
   */
  icon: string;
  /**
   * Size. Inherit, small, medium or large. Inherit value inherits styles from the value in the underlying slot.
   */
  size: "inherit" | "small" | "medium" | "large";
  /**
   * Caret
   */
  caret: boolean;
  /**
   * @internal
   * Indicates the component is used where aria-role menuitem is expected
   */
  menuItem: boolean;
  /**
   * Component lifecycle events.
   */
  componentWillLoad(): void;
  /**
   * render() function
   * Always the last one in the class.
   */
  render(): any;
}
