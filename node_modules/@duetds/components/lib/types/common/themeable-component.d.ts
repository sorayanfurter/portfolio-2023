import { ComponentInterface } from "../stencil-public-runtime";
import { DuetTheme } from "../common-types";
export interface ThemeableComponent extends ComponentInterface {
  /**
   * Theme of the card. Can be one of: "default", "turva".
   */
  theme: DuetTheme;
}
export declare const inheritGlobalTheme: (component: ThemeableComponent) => void;
